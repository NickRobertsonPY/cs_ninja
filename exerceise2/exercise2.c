#include <stdio.h>
#include <string.h>
#include <stdbool.h>

int main(void)

{
    int x;
    char y;
    _Bool z;

    printf("Size of int: %2ld bytes\n", sizeof(x));
    printf("Size of char: %2ld bytes\n", sizeof(y));
    printf("Size of bool: %2ld bytes\n", sizeof(z));


}

